# Truck Ticketing System - Billing Service

# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
  branches:
    include:
    - main
  paths:
    include:
    - Trident/*
    - SE.TruckTicketing/*
    exclude:
    - SE.TruckTicketing/TruckTicketing/*
    - SE.TruckTicketing/IntegrationService/*
    - SE.TruckTicketing/TokenService/*
    - SE.TruckTicketing/Tools/*

variables:
  projects: '**/SE.BillingService.Api.csproj'
  artifactName: 'SE.BillingService.Api'

pool:
  vmImage: 'windows-latest'

stages:
  - stage: 'Build'
    jobs:
    - job: 'Build'
      steps:
      - template: './templates/build.yml'
        parameters:
          projects: $(projects)
      - template: './templates/publish.yml'
        parameters:
          projects: $(projects)
          artifactName: $(artifactName)
